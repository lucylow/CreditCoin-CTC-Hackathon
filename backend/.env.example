APP_NAME="PediScreen AI - Backend"
DEBUG=true
API_KEY=dev-example-key
MONGO_URI=mongodb://localhost:27017/pediscreen
DB_NAME=pediscreen
UPLOAD_DIR=/tmp/pediscreen_uploads
MEDGEMMA_MODE=false
MEDGEMMA_MODEL_NAME=google/medgemma-2b-it
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Supabase JWT (optional - when frontend uses Supabase Auth, backend validates Bearer token)
# Get JWT Secret from Supabase Dashboard > Project Settings > API > JWT Secret
# SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
# SUPABASE_JWT_SECRET=your-jwt-secret

# DePIN + Edge AI (sensors, edge devices)
# DEPIN_DATABASE_URI=postgresql+pg8000://user:pass@host/depin
# Leave unset to use SQLite: sqlite:///./depin.db
# DEPIN_PIN_SENSOR_DATA=true
# DEPIN_ANCHOR_SENSOR_DATA=false
# DEPIN_ANCHOR_INFERENCE=false
